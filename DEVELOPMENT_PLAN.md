# نظام تطوير مشروع Agri-Nile Flow (Live-Remote Workflow) 🚀

هذا النظام مصمم لضمان عمل التطبيق دائماً على بيئة Cloudflare الحقيقية وتجنب أخطاء البيئة المحلية.

## 🛠 دورة التطوير الأساسية

### 1. تعديل الباك إند (The Worker)
كل تعديل تقوم به في ملفات الـ `src`:
*   **الأمر:** `wrangler deploy`
*   **ماذا يحدث؟** يتم رفع الكود الجديد فوراً للسيرفر.
*   **التحقق:** يمكنك اختبار الـ Endpoints مباشرة باستخدام الروابط الأونلاين.

### 2. تشغيل الفرونت إند (Vite)
لتطوير الواجهات وربطها بالباك إند اللايف:
*   **الأمر:** `npm run dev:web` (داخل فولدر web أو من الجذر)
*   **الرابط:** `http://localhost:3000` (أو 5173 حسب جهازك).
*   **الربط:** الـ Vite مهيأ الآن ليرسل أي طلب `/api/*` مباشرة إلى:
    `https://agri-nile-flow.zahranmalk2.workers.dev`

---

## 🗄 إدارة قاعدة البيانات (D1 Remote)

التعامل مع قاعدة البيانات يتم دائماً على النسخة الأونلاين (الـ Remote):

1.  **تحديث الجداول (Re-init):**
    لو عدلت في ملف `schema.sql` وعاوز تنفذه بالكامل:
    ```bash
    npm run db:init:remote
    ```

2.  **تنفيذ أوامر SQL سريعة:**
    ```bash
    wrangler d1 execute nawa-agro-ledger --remote --command="SELECT * FROM users;"
    ```

---

## 🚀 قواعد المشروع
1.  **لا يوجد قاعدة بيانات محلية:** لا تشغل نفسك بـ `wrangler dev` محلياً لتجنب مشاكل SQLite.
2.  **النشر أولاً:** ارفع الكود (`deploy`) ثم اختبر من الـ `localhost`.
3.  **السرية:** تأكد أن الـ `JWT_SECRET` مخزن في Cloudflare Dashboard أو `wrangler.toml` بشكل آمن.

---

## 📝 ملاحظة للمطور:
تم تعديل ملف `web/vite.config.ts` ليكون الـ Proxy دائماً على الرابط الأونلاين. أي طلب API من الـ localhost هيروح للوركر اللي أنت لسه عامل له Deploy.
