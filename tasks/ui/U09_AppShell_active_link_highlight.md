# AppShell navigation — verify active link highlighting works for all inventory sub-routes
- Open /inventory/movements: confirm "حركات المخزون" nav item is highlighted.
- Open /inventory/balances-detail: confirm correct nav item highlighted.
- Fix any nav item that uses exact match but should use startsWith match for sub-routes.
Verification:
- All inventory sub-pages have the correct nav item highlighted in the sidebar.
