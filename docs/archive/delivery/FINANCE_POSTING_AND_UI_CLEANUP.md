# توثيق تحديثات البنية التحتية المحاسبية وتنظيف الواجهات المالية

## 1. ملخص تنفيذي (Executive Summary)
تم إجراء عملية تنظيف وتوحيد شاملة للنظام المالي (Finance Module) للتأكد من أن جميع الحركات المالية تتم عبر مسار ترحيل واحد (Single Source of Truth) دون أي ازدواجية في القيود، بالإضافة إلى تبسيط واجهات المستخدم وإزالة الحقول التي لا تخدم الغرض المحاسبي.

---

## 2. تنظيف الواجهات المالية (UI & Form Cleanups)

### التعديلات التي تمت:
تمت إزالة الحقول التالية من نماذج `AddCashTransactionModal` (إضافة حركة خزينة) و `AddSupplierTransactionModal` (إضافة قيد مورد):
- `unit` (الوحدة)
- `quantity` (الكمية)
- `unit_price` (سعر الوحدة)
- الحسابات التلقائية المترتبة عليها (Qty × Price = Amount).

### الأسباب الجوهرية المحاسبية والتقنية (Why?):
1. **الاستقلالية المحاسبية (Separation of Concerns):** الخزينة والموردين في طبيعتهما المالية يتعاملان مع **"مبالغ نقدية" (Amounts)** فقط كمتحصلات أو مدفوعات أو تسويات.
2. **منع الازدواجية المخزنية:** إدخال كميات وأسعار بضاعة يجب أن يقتصر دائماً وأبداً على دورة المخازن (Inventory - GRN / Issue). إتاحة هذه الحقول في شاشات المورد المباشرة أو الخزينة يعطي انطباعاً زائفاً للمستخدم بأنه يدخل بضاعة، في حين أن هذا الإدخال **لا يُسمِّع** إطلاقاً في رصيد المخزن الفعلي (`inventory_movements`)، مما يسبب خللاً وتضارباً في البيانات بين التقارير المخزنية والتقارير المالية.
3. **تبسيط تجربة المستخدم (UX):** تقليل الحقول المربكة يقلل من الأخطاء البشرية. تم الاستعاضة عن كل هذه الحقول بحقل مباشر وهو `Amount` (المبلغ الإجمالي).

---

## 3. تنظيف قاعدة البيانات (Database Schema Cleanup)

### التعديلات التي تمت:
تمت إزالة الحقول المهجورة أعلاه (`unit`, `quantity`, `unit_price`) من:
- **ملف التأسيس `schema.sql`:** من جداول `cash_transactions` و `supplier_transactions`.
- **مُعالجات الإدخال بالخادم (Backend APIs):** تم مسحها من جمل الـ `INSERT` في كل من `src/lib/finance/cash_movement.ts` و `src/api/suppliers.ts`.

### الأسباب:
1. التخلص من تراكم البيانات الميتة (Dead Data).
2. تقليل حجم البيانات المرسلة والمستقبلة.
3. منع المطورين في المستقبل من الاعتماد على حقول وهمية لا أثر لها في النظام.

---

## 4. إغلاق ثغرات الترحيل المالي (Posting Integration Fixes)

### الثغرة الأولى: غياب الترحيل للقيود المباشرة في الخزينة
- **المشكلة:** الحركات النقدية المباشرة (مثل "مصروف عام" أو "إيداع" من الخزينة) كانت تُسجل بنجاح في جدول النقدية `cash_transactions` ولكنها **لا تُوّلد أي قيد محاسبي في دفتر الأستاذ (GL - journal_entries)**.
- **الحل:** تم التعديل على `src/lib/finance/cash_movement.ts`؛ بحيث أنه بعد تسجيل الحركة في الخزينة، يتحقق النظام إذا ما كانت الحالة `posted`، ليقوم تلقائياً باستدعاء `resolveCashLedger` وبناء القيد المحاسبي السليم ثم ربط `journal_entry_id` الناتج بالحركة النقدية.

### الثغرة الثانية: خطر الازدواجية في ترحيل الموردين (Double-Entry Hazard)
- **المشكلة:** بما أننا قمنا ببرمجة الخزينة لتقوم بإنشاء قيود يومية، ظهر خطر الازدواجية. لأن دورة المشتريات (Purchasing) ودورة الموردين (Suppliers) يقومان بإنشاء قيد يومية بأنفسهم (عبر `resolveSupplierPayment` أو `resolveSupplierInvoice`)، ثم يقومون بتسجيل حركة في الخزينة للحفاظ على دقة أرصدة الخزينة. هذا كان سيؤدي لتسجيل القيد المحاسبي مرتين (مرة للمورد ومرة للخزينة).
- **الحل:** تم إضافة مُعامل حماية (Guard Flag) باسم `skipGlPosting: true` في دالة `CashMovementInput`.
  - أصبح الآن مسار المورد يُخبر الخزينة: *"سجلي الحركة النقدية عندك لضبط الأرصدة، ولكن لا تقومي بإنشاء قيد GL لأنني قمت بذلك مسبقاً"*.
  - هذا يحقق مبدأ (One Source of Truth) بامتياز.

### إنجازات إضافية في الترحيل:
- **حسابات الشركاء:** تم إعادة تمرير `contra_account` عبر الـ API للخزينة لضمان أن إضافة أو سحب رأسمال الشريك يُقيّد بشكل صحيح في حساب (Equity - حقوق الملكية) بدلاً من أن يضيع في حسابات المصروفات أو الإيرادات الافتراضية.

---

## 5. حالة النظام الآن (System Status)
- ✅ الواجهات المالية أصبحت مخصصة للمعاملات النقدية فقط وبدون تداخل مع المخازن.
- ✅ جميع معاملات الخزينة (حتى المباشرة) تُولد قيوداً محاسبية نظامية.
- ✅ لا يوجد أي ازدواجية في القيود اليومية (Duplicate GL Entries) بفضل نظام فصل الترحيل الذكي (`skipGlPosting`).
- ✅ الملفات التي تمت صيانتها:
  - `AddCashTransactionModal.tsx`
  - `AddSupplierTransactionModal.tsx`
  - `schema.sql`
  - `src/lib/finance/cash_movement.ts`
  - `src/lib/finance/resolvers/cash.ts`
  - `src/api/suppliers.ts`
