# تقرير الهندسة العكسية وجودة البيانات — شركة نواة المستقبل

**التاريخ:** 2026-04-21  
**نطاق التحليل:** السنة المالية 2025 – 2026  
**الملفات المحللة:**  
1. `الموردين والعملاء نواة المستقبل2025-2026.xlsx` (3.2 MB)  
2. `خزينة نواة المستقبل 2025-2026.xlsx` (3.0 MB)  
3. `مخازن نواة المستقبل2025-2026.xlsx` (2.8 MB)  

**الأسلوب:** قراءة برمجية كاملة لكل شيت باستخدام `openpyxl` في وضع `read_only=True`، ثم استنباط الصف الحقيقي للعناوين، تصنيف كل شيت، واستخلاص سلوك كل عمود على كل البيانات (لا عينة).

---

## 1. الخلاصة التنفيذية (TL;DR)

| المقياس | القيمة |
|---|---:|
| عدد الشيتات الكلي (الملفات الثلاثة) | **19** |
| شيتات تم تصنيفها كمصدر للبيانات الخام (Fact) | **6** (شيت واحد للحركات وآخر للأكواد في كل ملف) |
| شيتات Pivot/Report يجب تجاهلها | **13** |
| إجمالي الصفوف في شيتات الحركات الثلاث | **46,034** |
| صفوف حركات حقيقية (بعد التنقية) | **1,082** فقط |
| **نسبة الصفوف الشبحية (Formula Spillover)** | **~97.6 %** |
| صفوف بتاريخ مستقبلي بعد تاريخ اليوم | **52** (يحتاج مراجعة يدوية) |
| أعمدة حسابية / أرصدة تراكمية يجب حذفها | **8** أعمدة |

**السبب الجذري لأغلب مشاكل الجودة:** الملفات استخدمت `VLOOKUP/INDEX-MATCH` تم سحبها يدوياً حتى الصف 20,000 — كل صف بعد آخر حركة حقيقية يرجع `#N/A` أو أصفار. يجب أن تُفَلتَر هذه الصفوف قبل الترحيل.

---

## 2. الخطوة الأولى: مسح وتصنيف الشيتات

### 2.1 ملف الخزينة (`خزينة نواة المستقبل 2025-2026.xlsx`)

| اسم الشيت | الصفوف × الأعمدة | التصنيف | الملاحظات |
|---|---:|---|---|
| `البيان` | 19,915 × 26 | **FACT** | شيت حركات الخزينة الرئيسي (قيد عام يومي) |
| `الاكواد` | 2,235 × 14 | **FACT / Master** | 3 جداول أكواد متجاورة (موردين، مصروفات، مراكز) |
| `اجمالي الموردين` | 18 × 4 | REPORT | تجميعي |
| `كشف حساب دفعات مورد` | 45 × 5 | REPORT | كشف حساب |
| `مساهمة الشركاء` | 45 × 5 | REPORT | Pivot بعمودَي `Sum of دائن/مدين` |

### 2.2 ملف المخازن (`مخازن نواة المستقبل2025-2026.xlsx`)

| اسم الشيت | الصفوف × الأعمدة | التصنيف | الملاحظات |
|---|---:|---|---|
| `البيانات` | 10,569 × 32 | **FACT** | شيت حركات المخازن (وارد/منصرف) |
| `الكود` | 4,839 × 19 | **FACT / Master** | 4 جداول أكواد (أصناف، موردين، مراكز، SUB) |
| `ارصدة المخازن` | 971 × 15 | REPORT (Pivot) | يحوي أعمدة `Sum of كمية / قيمة الرصيد` |
| `بطاقة صنف` | 2,496 × 12 | REPORT (Pivot) | بطاقة صنف محسوبة |
| `تكاليف مراكز` | 1,307 × 11 | REPORT | تجميع بعمود `Total` |
| `البيان اليومي` | 364 × 8 | REPORT (Pivot) | `Sum of كمية الوارد/المنصرف` |
| `كشف حساب مورد` | 293 × 8 | REPORT | كشف حساب مورد |
| `Sheet1` | 39 × 5 | REPORT | ملخص أرصدة مخازن |

### 2.3 ملف الموردين والعملاء (`الموردين والعملاء نواة المستقبل2025-2026.xlsx`)

| اسم الشيت | الصفوف × الأعمدة | التصنيف | الملاحظات |
|---|---:|---|---|
| `البيان` | 15,582 × 67 | **FACT** | شيت حركات الموردين/العملاء — ⚠️ منهم **36 عموداً شبحياً فارغاً** (من 31 إلى 66) |
| `الكود` | 2,156 × 14 | **FACT / Master** | 4 جداول أكواد متجاورة |
| `أرصدة الموردين` | 157 × 7 | REPORT (Pivot) | `Sum of دائن/مدين/الرصيد` |
| `كشف حساب مفصل` | 2,930 × 15 | REPORT (Pivot) | ديناميكي |
| `كشف حساب مجمل` | 2,077 × 12 | REPORT (Pivot) | |
| `مركز التكلفة` | 1,359 × 7 | REPORT (Pivot) | `Sum of الكميه/القيمه` |

> **قاعدة تشغيلية:** كل شيت اسمه يحوي `كشف / أرصدة / بطاقة / Sum of / Total / ميزان` تم استبعاده من الترحيل — يُعاد بناؤه بعد تحميل البيانات باستخدام `Views / SELECT` في قاعدة البيانات.

---

## 3. الخطوة الثانية: الهندسة العكسية للهيكل (Schema Profiling)

### 3.1 شيت الخزينة — `البيان` (صف العناوين الحقيقي: **5**)

| # | اسم العمود | النوع المنطقي | ‎% NULL | ملاحظة |
|---:|---|---|---:|---|
| 0 | التاريخ | `DATE` | 99.65 % | ← فارغ في الصفوف الشبحية |
| 1 | الحالة | `CHAR(1)` | 99.65 % | 'د' / 'م' |
| 2 | رقم المستند | `BIGINT` | 99.65 % | |
| 3 | المستلم / المسلم | `NVARCHAR(80)` | 99.65 % | 12 قيمة مختلفة فقط |
| 4 | البيان | `NVARCHAR(255)` | 99.65 % | |
| 5 | الموسم / الخدمة | `—` | 100 % | 🗑️ فارغ كلياً |
| 6 | ملاحظات | `NVARCHAR(255)` | 99.73 % | |
| 7 | كود المورد | `BIGINT (FK)` | 99.73 % | |
| 8 | كود المركز | `BIGINT (FK)` | 99.93 % | |
| 9 | كود المصروف | `BIGINT (FK)` | 99.96 % | |
| 10 | كود SUB | `BIGINT (FK)` | 99.96 % | |
| 11 | الوحدة | `NVARCHAR(20)` | 0 % | ⚠️ "مبلغ" ثابت في كل الصفوف الشبحية |
| 12 | الكمية | `INT` | 0 % | ⚠️ 1 ثابت في كل الصفوف الشبحية |
| 13 | السعر | `DECIMAL(18,4)` | 99.65 % | |
| 14 | القيمة | `DECIMAL(18,4)` | 0 % | ⚠️ 0 في الصفوف الشبحية |
| 15 | مدين | `DECIMAL(18,4)` | 0 % | |
| 16 | دائن | `DECIMAL(18,4)` | 0 % | |
| **17** | **الرصيد** | — | 0 % | 🗑️ **رصيد تراكمي محسوب — احذف** |
| 18 | المورد (lookup) | — | 0 % | 🗑️ **15 صف #N/A، احذف وأعد الاستخراج من FK** |
| 19 | المركز (lookup) | — | 0 % | 🗑️ 55/69 قيمة `#N/A` |
| 20 | المصروف (lookup) | — | 0 % | 🗑️ 62/69 `#N/A` |
| 21 | SUB (lookup) | — | 0 % | 🗑️ 62/69 `#N/A` |
| 22 | الخدمة (lookup) | — | 0 % | 🗑️ |
| 23 | عام الانشاء | — | 99.65 % | 🗑️ `YEAR(التاريخ)` |
| 24 | شهر الإنشاء | — | 99.65 % | 🗑️ `MONTH(التاريخ)` |
| 25 | (بدون اسم) | — | 100 % | 🗑️ |

### 3.2 شيت المخازن — `البيانات` (صف العناوين: **3**)

| # | اسم العمود | النوع المنطقي | ‎% NULL | ملاحظة |
|---:|---|---|---:|---|
| 0 | البرنامج | `—` | 100 % | 🗑️ |
| 1 | السنة | — | 93.37 % | 🗑️ `YEAR()` |
| 2 | الشهر | — | 93.37 % | 🗑️ `MONTH()` |
| 3 | التاريخ | `DATE` | 93.37 % | |
| 4 | المخزن | `NVARCHAR(40)` | 0 % | "اسمدة/مبيدات/تقاوي وبذور/شبكات ري/قطع غيار/..." |
| 5 | النوع | `ENUM` | 93.37 % | 'اضافة' / 'صرف' |
| 6 | رقم المستند | `BIGINT` | 93.37 % | |
| 7 | رقم الفاتورة | `—` | 100 % | 🗑️ فارغ كلياً |
| 8 | رقم امر التوريد | `—` | 100 % | 🗑️ فارغ كلياً |
| 9 | كود المورد | `BIGINT (FK)` | 99.16 % | |
| 10 | المورد (lookup) | — | 0 % | 🗑️ 611/700 صف #N/A |
| 11 | كود الصنف | `BIGINT (FK)` | 93.37 % | |
| 12 | الصنف (lookup) | — | 0 % | 🗑️ |
| 13 | الوحدة | `NVARCHAR(20)` | 0 % | |
| 14 | العبوه | `NVARCHAR(30)` | 95.57 % | ⚠️ whitespace: `'عبوة '` |
| 15 | السعة | `DECIMAL` | 93.38 % | |
| 16 | العدد | `DECIMAL` | 93.38 % | |
| 17 | كود الحساب | `BIGINT (FK)` | 94.20 % | |
| 18 | الحساب الرئيسي (lookup) | — | 0 % | 🗑️ |
| 19 | كود مركز التكلفة | `BIGINT (FK)` | 94.21 % | |
| 20 | مركز التكلفة (lookup) | — | 0 % | 🗑️ 89 #N/A |
| 21 | الكود (SUB) | `BIGINT (FK)` | 99.92 % | |
| 22 | SUB (lookup) | — | 0 % | 🗑️ **695/700 `#N/A`** (عمود معطل تقريباً) |
| 23 | الكمية | `DECIMAL` | 93.37 % | |
| 24 | الفئة (unit price) | `DECIMAL` | 93.67 % | |
| 25 | كمية الوارد | `DECIMAL` | 0 % | يجب فلترتها بـ `WHERE نوع='اضافة'` |
| 26 | كمية المنصرف | `DECIMAL` | 0 % | `WHERE نوع='صرف'` |
| **27** | **الرصيد** | — | 0 % | 🗑️ رصيد تراكمي |
| 28 | قيمة الوارد | `DECIMAL` | 0 % | |
| 29 | قيمة المنصرف | `DECIMAL` | 0 % | |
| **30** | **قيمة الرصيد** | — | 0 % | 🗑️ رصيد تراكمي |
| 31 | ملاحظات | — | 100 % | 🗑️ |

### 3.3 شيت الموردين/العملاء — `البيان` (صف العناوين: **3**)

الشيت يُدَّعى أنه 67 عموداً لكن من **العمود 31 إلى 66** كلهم فارغون كلياً (36 عموداً شبحياً). الحقول الفعلية هي أول 31 عموداً فقط:

| # | اسم العمود | النوع المنطقي | ‎% NULL | ملاحظة |
|---:|---|---|---:|---|
| 0 | التاريخ | `DATE` | 97.99 % | |
| 1 | النوع | `CHAR(1)` | 97.99 % | 'د' / 'م' |
| 2 | كود المورد | `BIGINT (FK)` | 97.99 % | |
| 3 | المورد / العميل (lookup) | — | 0 % | 🗑️ (resolvable) |
| 4 | البيان | `NVARCHAR(255)` | 97.99 % | |
| 5 | نوع المستند | `NVARCHAR(60)` | 97.99 % | مستخلص / محضر / اذن اضافة / اذن صرف |
| 6 | رقم المستند | `BIGINT` | 98.00 % | 1 قيمة مفقودة |
| 7 | المصروف | `NVARCHAR(40)` | 98.62 % | |
| 8 | المعدة | `NVARCHAR(40)` | 99.30 % | |
| 9 | الخدمة | `NVARCHAR(255)` | 97.99 % | |
| 10 | كود الحساب | `BIGINT (FK)` | 98.62 % | |
| 11 | الحساب الرئيسي (lookup) | — | 0 % | 🗑️ 99 `#N/A` |
| 12 | كود البيقوت | `BIGINT (FK)` | 98.62 % | (تهجئة خاطئة لـ "البيفوت") |
| 13 | البيفوت (lookup) | — | 0 % | 🗑️ 99 `#N/A` |
| 14 | الكود | `BIGINT` | 99.99 % | |
| 15 | SUB (lookup) | — | 0 % | 🗑️ **100 % `#N/A`** |
| 16 | الوحدة | `NVARCHAR(20)` | 97.99 % | ⚠️ `'كجم' vs 'كجم '` whitespace dup |
| 17 | الكميه | `DECIMAL` | 97.99 % | |
| 18 | السعر | `DECIMAL` | 98.10 % | |
| 19 | القيمه | `DECIMAL` | 0 % | |
| 20 | دائن | `DECIMAL` | 0 % | |
| 21 | مدين | `INT` | 0 % | |
| 22 | الشيكات | `DECIMAL` | 0 % | |
| 23 | تاريخ الاستحقاق | `DATE` | 100 % | (لم يُستخدم حتى الآن) |
| **24** | **الرصيد بدون شيكات** | — | 0 % | 🗑️ رصيد تراكمي |
| **25** | **الرصيد بالشيكات** | — | 0 % | 🗑️ رصيد تراكمي |
| 26 | تاريخ صرف الشيك | `DATE` | 100 % | |
| 27 | سنة الانشاء | — | 97.99 % | 🗑️ |
| 28 | شهر الانشاء | — | 97.99 % | 🗑️ |
| 29 | الخدمة (مكرر) | — | 0 % | 🗑️ تكرار |
| 30 | ملاحظات | `NVARCHAR(500)` | 100 % | يُحتفظ بهيكله |
| 31-66 | (أعمدة شبحية) | — | 100 % | 🗑️ **36 عموداً فارغ كلياً — احذف** |

---

## 4. الخطوة الثالثة: فحص جودة البيانات واكتشاف الشوائب

### 4.1 أكبر مشكلة: Formula Spillover (الصفوف الشبحية)

الثلاث شيتات الأساسية تحوي معادلات `VLOOKUP` مسحوبة يدوياً لآلاف الصفوف فوق عدد الحركات الفعلية. مثال حي من شيت الخزينة:

**صف حقيقي (row 6):**
```
{0: '2025-11-06', 1: 'د', 2: '6251', 3: 'القوات الجوية', 4: 'جهاز مستقبل مصر...',
 14: 1000000, 15: 0, 16: 1000000, 17: 1000000, 18: '#N/A'}
```

**صف شبحي (row 505، يتكرر ~19,800 مرة):**
```
{11: 'مبلغ', 12: 1, 14: 0, 15: 0, 16: 0, 17: 19801, 18: '#N/A', 19: '#N/A',
 20: '#N/A', 21: '#N/A', 22: '#N/A'}
```
العمود 17 (الرصيد) يخزّن نفس الرقم 19,801 مجمداً (وهو الرصيد بعد آخر حركة)، بينما كل أعمدة الـ lookup ترجع `#N/A`. 

**الأثر الكمّي:**

| الشيت | إجمالي الصفوف | صفوف حقيقية | صفوف شبحية | ‎% شبحي |
|---|---:|---:|---:|---:|
| `treasury.البيان` | 19,910 | 69 | 19,841 | **99.65 %** |
| `inventory.البيانات` | 10,561 | 700 | 9,861 | **93.37 %** |
| `suppliers.البيان` | 15,563 | 313 | 15,250 | **98.00 %** |

**قاعدة الفلترة الإلزامية:** `WHERE التاريخ IS NOT NULL AND التاريخ NOT LIKE '#N/A%'` قبل أي `INSERT`.

### 4.2 أعمدة الـ Lookup المعطلة (‎`#N/A‎`)

صفوف حقيقية لها تاريخ لكن أعمدة الـ lookup ترجع `#N/A` — أي أن كود المورد/المركز موجود في شيت الحركات لكنه غير موجود في شيت الأكواد. **ترحيلها سيفقد المعلومة** إذا لم نحتفظ بالـ FK ونعيد استخراج الاسم من الجدول المرجعي.

| الملف | العمود | عدد `#N/A` / الصفوف الحقيقية | التشخيص |
|---|---|---:|---|
| treasury.البيان | SUB | 62 / 69 | كود SUB غير مسجل في جدول الأكواد |
| treasury.البيان | المصروف | 62 / 69 | كود المصروف غير مسجل |
| treasury.البيان | المركز | 55 / 69 | كود المركز غير مسجل |
| treasury.البيان | المورد | 15 / 69 | 15 كود مورد غير موجود |
| inventory.البيانات | SUB | **695 / 700** | الجدول شبه معطل |
| inventory.البيانات | المورد | 611 / 700 | الأغلب حركات "صرف" ليس لها مورد (طبيعي) |
| inventory.البيانات | مركز التكلفة | 89 / 700 | |
| inventory.البيانات | الحساب الرئيسي | 88 / 700 | |
| suppliers.البيان | SUB | **313 / 313** | العمود معطل بالكامل |
| suppliers.البيان | الحساب الرئيسي | 99 / 313 | |
| suppliers.البيان | البيفوت | 99 / 313 | |

**توصية:** بعد الترحيل، شغّل الاستعلام التالي لاكتشاف كل الأكواد اليتيمة (orphans):

```sql
-- كل كود مورد في الحركات غير موجود في جدول الموردين
SELECT DISTINCT supplier_code FROM supplier_transactions_clean
WHERE supplier_code IS NOT NULL
  AND supplier_code NOT IN (SELECT code FROM suppliers);
```

### 4.3 الصفوف ذات التواريخ المستقبلية (بعد 2026-04-21)

| الملف | عدد الصفوف المستقبلية | نطاق التاريخ | |
|---|---:|---|---|
| inventory.البيانات | **46** | 2026-12-28 → 2026-12-31 | 🚩 |
| suppliers.البيان | **6** | 2026-12-28 | 🚩 |
| treasury.البيان | 0 | — | ✅ |

**عينة:**
```
inventory row:  2026-12-28  اسمدة  اضافة  32913  ...  
suppliers row:  2026-12-28  د  20900353  شركة عرفة للتصدير...  مشتريات اسمدة ومبيدات
```

هذه يمكن أن تكون:  
1. إدخال خاطئ (2026 بدل 2025)،  
2. قيود مخططة (مستخلصات مؤجلة)،  
3. أو خطأ صيغة التاريخ.

**توصية:** عزلها في جدول `pending_review` بدل إدراجها مباشرة.

### 4.4 Whitespace & Encoding Duplicates

الوحدات/التصنيفات التالية كُتبت بتهجئتين مختلفتين وتظهر كقيمتين فريدتين في أي `GROUP BY`:

| العمود | المتغيرات المكتشفة | الحل |
|---|---|---|
| `suppliers.الوحدة` | `'كجم'` vs `'كجم '` | `TRIM()` |
| `suppliers.الوحدة` | `'فدان '`, `'ساعه '`, `'مبلغ '` | `TRIM()` |
| `inventory.العبوة` | `'عبوة '` vs `'عبوة'` | `TRIM()` |
| `suppliers.البيفوت` و `inventory.مركز التكلفة` | `' بيفوت رقم 720 بوستر129'` (مع فراغ ابتدائي) | `TRIM()` + `REGEXP_REPLACE(' +', ' ')` |

لم تُكتَشف فروقات Unicode أخرى (مثل "ي" vs "ى"، "ة" vs "ه") داخل أسماء الموردين أو الأصناف في الشيتات الثلاث — إجمالي الموردين الفريدين **7 فقط**، إجمالي الأصناف الفريدة **~61** (قاعدة بيانات صغيرة جداً بالنسبة لحجم الملفات).

### 4.5 Data-Type Mismatches

لم يُكتَشف أي **نص عربي داخل عمود رقمي** في الشيتات الثلاث (لا يوجد "عشرون" بدلاً من 20). كل القيم الرقمية إما خلايا `int` أو `float` سليمة أو `#N/A`. التواريخ أيضاً كلها `datetime` حقيقية في Excel (ليست نصوصاً).

### 4.6 اتساق صيغ التواريخ

كل القيم في عمود `التاريخ` مخزنة كـ **Excel date serial** (نوع `datetime`)، والعرض في النافذة هو `YYYY-MM-DD HH:MM:SS`. لا يوجد تداخل `DD/MM` مع `MM/DD` — المشكلة هنا ليست الصيغة بل **قيمة التاريخ** (صفوف 2026-12-28). نطاق التواريخ الإجمالي:

| الملف | أقدم تاريخ | أحدث تاريخ |
|---|---|---|
| treasury | 2025-11-06 | 2026-04-01 |
| inventory | 2025-11-24 | 2026-12-31 |
| suppliers | 2025-11-12 | 2026-12-31 |

---

## 5. الخطوة الرابعة: التوصيات المعمارية

### 5.1 أعمدة يجب حذفها من الترحيل

| الفئة | الأعمدة |
|---|---|
| **Running Balances** | `treasury.الرصيد`, `suppliers.الرصيد بدون شيكات`, `suppliers.الرصيد بالشيكات`, `inventory.الرصيد`, `inventory.قيمة الرصيد` |
| **Computed Date Parts** | `treasury.عام الانشاء`, `treasury.شهر الإنشاء`, `suppliers.سنة الانشاء`, `suppliers.شهر الانشاء`, `inventory.السنة`, `inventory.الشهر` |
| **Denormalized Lookups** | `المورد (name)`, `المركز (name)`, `المصروف (name)`, `الحساب الرئيسي`, `البيفوت`, `SUB` — تُستخرج عند الحاجة من جداول الأكواد |
| **Ghost / Empty Columns** | `suppliers` cols 31–66 (36 عموداً)، `inventory.البرنامج`, `inventory.رقم الفاتورة`, `inventory.رقم امر التوريد`, `inventory.ملاحظات` |

هذه الأعمدة تُعاد توليدها كـ **Views** أو **Computed Columns** في قاعدة البيانات.

### 5.2 الهيكل العلائقي المقترح

```
┌─────────────────┐       ┌──────────────────────────┐
│ suppliers       │◄──┐   │ cash_transactions_clean  │
│ (code, name)    │   │   │ (treasury journal)       │
└─────────────────┘   │   └──────────────────────────┘
┌─────────────────┐   │   ┌──────────────────────────┐
│ accounts        │◄──┼───│ supplier_transactions_clean (AP)  │
│ (code, name)    │   │   └──────────────────────────┘
└─────────────────┘   │   ┌──────────────────────────┐
┌─────────────────┐   ├───│ inventory_movements_clean │
│ cost_centers    │◄──┤   └──────────────────────────┘
│ expense_types   │◄──┤                    │
│ sub_locations   │◄──┤                    ▼
│ items           │◄──┘       ┌─────────────────────┐
└─────────────────┘           │ gl_vouchers         │─┐
                              │ gl_entries (DR/CR)  │◄┘
                              └─────────────────────┘
                                       │
                                       ▼
                   ┌──────────────────────────────────┐
                   │ v_supplier_balance   (View)      │
                   │ v_item_stock         (View)      │
                   │ v_trial_balance      (View)      │
                   │ v_cash_position      (View)      │
                   └──────────────────────────────────┘
```

### 5.3 توحيد النظام كـ Double-Entry General Ledger

لكل حركة من الأنظمة الفرعية الثلاثة يتم توليد **قيد مزدوج** في `gl_vouchers` + `gl_entries` حسب المنطق التالي:

| المصدر | حالة | المدين DR | الدائن CR |
|---|---|---|---|
| خزينة مدفوعات (`م`) | دفعة لمورد | `حساب المورد` | `صندوق / بنك` |
| خزينة واردات (`د`) | استلام نقدي | `صندوق / بنك` | `حساب مساهمة شركاء / رأس مال` |
| مورد فاتورة (`د`) | أصل الاستحقاق | `مصروف / مخزون` | `ذمم موردين` |
| مورد دفعة (`م`) | سداد مستحق | `ذمم موردين` | `صندوق / بنك` |
| مخزن إضافة | شراء وارد | `مخزون (قيمة الوارد)` | `ذمم موردين` |
| مخزن صرف | صرف للحقل | `مصروف مركز تكلفة` | `مخزون (قيمة المنصرف)` |

هذا الجدول البسيط يستبدل 3 شيتات Excel + 13 pivot بجدول موحّد واحد مع قيد التحقق:

```sql
-- كل قسيمة يجب أن تكون متوازنة
SELECT voucher_id, SUM(debit) - SUM(credit) AS delta
FROM gl_entries
GROUP BY voucher_id
HAVING delta <> 0;  -- يجب أن يرجع Zero Rows
```

### 5.4 ملخص SQL DDL المقترح (SQLite / D1)

راجع الملف المصاحب `ledger_migration.sql` — يحوي:
1. `stg_*` tables لاستقبال البيانات الخام دون تحويل
2. `*_clean` tables بعد الفلترة والتطبيع
3. `gl_vouchers` + `gl_entries` للقيد المزدوج الموحّد
4. 4 Views لإعادة إنتاج التقارير (أرصدة، ميزان، كشوف)
5. 3 استعلامات Assertion للتحقق بعد الترحيل

**مقتطف — Views لاستبدال الأرصدة التراكمية:**

```sql
CREATE VIEW v_supplier_balance AS
SELECT s.code, s.name,
       SUM(e.credit) - SUM(e.debit) AS outstanding_balance,
       MAX(e.entry_date) AS last_movement
FROM suppliers s
LEFT JOIN gl_entries e ON e.supplier_code = s.code
GROUP BY s.code, s.name;

CREATE VIEW v_item_stock AS
SELECT i.code, i.name,
       SUM(COALESCE(qty_in,0) - COALESCE(qty_out,0))   AS qty_on_hand,
       SUM(COALESCE(value_in,0) - COALESCE(value_out,0)) AS stock_value
FROM items i
LEFT JOIN inventory_movements_clean m ON m.item_code = i.code
GROUP BY i.code, i.name;

CREATE VIEW v_trial_balance AS
SELECT a.code, a.name,
       SUM(e.debit) AS total_debit,
       SUM(e.credit) AS total_credit,
       SUM(e.debit) - SUM(e.credit) AS net
FROM accounts a
LEFT JOIN gl_entries e ON e.account_code = a.code
GROUP BY a.code, a.name;
```

---

## 6. خطة التنفيذ المقترحة (Migration Runbook)

1. **Extract** — ETL بلغة Python (استخدام `openpyxl read_only=True`):
   - افتح كل شيت `FACT` → اقرأ من `header_row + 1` حتى آخر صف بتاريخ غير NULL
   - أرسل إلى `stg_*` مباشرة كنص خام (تُحفظ ‎#N/A‎ كما هي)
2. **Load to Staging** — `BULK INSERT` في جداول `stg_cash_txn`, `stg_supplier_txn`, `stg_inventory_mv`
3. **Transform & Promote** — نفّذ الـ `INSERT … SELECT` من `ledger_migration.sql` الذي يطبّق:
   - `DATE(raw_date)` لتحويل التواريخ
   - `TRIM()` على كل الحقول النصية
   - `CAST(... AS INTEGER/REAL)` للأرقام
   - فلترة `raw_date IS NOT NULL AND DATE(raw_date) <= DATE('now')`
4. **Review Future-Dated** — انقل الـ 52 صفاً المستقبلية إلى جدول `pending_review`
5. **Build GL** — Script يولّد `gl_vouchers` + `gl_entries` من كل `*_clean` حسب جدول الخريطة في §5.3
6. **Assertions** — شغّل 3 استعلامات التحقق في نهاية `ledger_migration.sql`
7. **Reconcile vs Excel** — قارن `v_supplier_balance` مع شيت `أرصدة الموردين` الأصلي — يجب أن تتطابق الأرقام ±0.01

---

## 7. عدد الصفوف المتوقع بعد الترحيل

| الجدول الهدف | قبل التنقية | **بعد التنقية** | ملاحظة |
|---|---:|---:|---|
| `cash_transactions_clean` | 19,910 | **69** | 99.65 % نقصان |
| `supplier_transactions_clean` | 15,563 | **307** | (313 − 6 مستقبلية) |
| `inventory_movements_clean` | 10,561 | **654** | (700 − 46 مستقبلية) |
| `pending_review` (مستقبلية) | — | **52** | |
| `gl_entries` (جميع القيود المزدوجة) | — | ≈ **2,060** | ~1,030 قسيمة × قيدان لكل منها |

---

## 8. الملفات المصاحبة

| الملف | الوصف |
|---|---|
| `DATA_QUALITY_MIGRATION_REPORT.md` | هذا التقرير |
| `migration_manifest.json` | خريطة تحويل كل عمود مصدر → عمود هدف + سبب الحذف |
| `ledger_migration.sql` | DDL كامل (Staging + Clean + GL + Views + Assertions) |
