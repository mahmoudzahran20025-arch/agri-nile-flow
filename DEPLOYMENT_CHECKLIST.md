# قائمة الفحص — Agri-Nile Flow 🚀

**التاريخ:** 20 أبريل 2026  
**الحالة:** جاهز للإنتاج ✅

---

## 1️⃣ التطبيق الفوري (Production Ready) ✅

### الخوادم المنشورة

| المكون | الرابط | الحالة |
|------|--------|--------|
| Backend | `https://agri-nile-flow.mahm-zahran22.workers.dev` | ✅ مُنشر |
| Frontend | `https://dcbb1029.agri-nile-flow-lake.pages.dev` | ✅ مُنشر |
| Database | `agri-nile-flow-data-lake` (D1) | ✅ جاهزة |

### بيانات الدخول الافتراضية

```
البريد الإلكتروني: admin@nawa.eg
كلمة المرور: Admin@2025
الشركة: نواة المستقبل (NM-001)
الدور: super_admin
```

### الميزات المتاحة

- ✅ تسجيل الدخول (3-step flow)
- ✅ لوحة التحكم (KPIs + Charts)
- ✅ إدارة الموردين (CRUD + Statements)
- ✅ دفتر اليومية النقدية
- ✅ إدارة الشركاء (رأس المال + الحساب الجاري)
- ✅ حركات المخزون
- ✅ التقارير المالية الشاملة
- ✅ إدارة المستخدمين
- ✅ تغيير كلمة المرور

---

## 2️⃣ استيراد البيانات من Excel 📊

### التحضير

#### الخطوة 1: جهّز ملفات Excel الثلاثة

ضع الملفات في مجلد `migrate/`:

```
migrate/
├── suppliers.xlsx       (الموردين + حركات الحسابات)
├── treasury.xlsx        (دفتر اليومية)
├── inventory.xlsx       (حركات المخزون)
└── ...
```

#### الخطوة 2: تحديد بنية الملفات

افتح `migrate/config.js` وعدّل الأعمدة لتطابق ملفاتك:

**مثال من الملف:**

```javascript
export const SUPPLIERS_CONFIG = {
  file: 'suppliers.xlsx',
  masterSheet: {
    name: 'الموردين',      // اسم الـ sheet
    startRow: 2,            // صف البيانات (1 = الهيدر، 2 = البيانات)
    columns: {
      code:     'A',        // كود المورد
      name:     'B',        // الاسم
      activity: 'C',        // النشاط (اختياري)
    },
  },
  transSheet: {
    name: 'الحركات',
    startRow: 2,
    columns: {
      supplier_code:    'A',
      transaction_date: 'B',    // DD/MM/YYYY
      entry_type:       'C',    // 'م' = مدين، 'د' = دائن
      amount:           'J',    // القيمة
    },
  },
}
```

**عرّف الأعمدة حسب ملفاتك الفعلية!**

#### الخطوة 3: التشغيل

```bash
# انتقل إلى مجلد migrate
cd migrate

# ثبّت الاعتمادات
npm install

# استيراد البيانات (بهذا الترتيب مهم!)
node import.js suppliers    # أولاً — الموردين
node import.js treasury     # ثانياً — الخزينة
node import.js inventory    # ثالثاً — المخزون

# أو الكل دفعة واحدة
node import.js all
```

**ملاحظة:** يجب استيراد الموردين أولاً لأن حركات الخزينة قد تشير إليهم.

#### الخطوة 4: التحقق من الأخطاء

إذا حدث خطأ:

```
❌ Sheet "الموردين" not found in suppliers.xlsx
```

هذا يعني أن اسم الـ sheet في `config.js` لا يطابق الملف الفعلي. اضبطه!

---

## 3️⃣ اختبار التطبيق 🧪

### الاختبارات اليدوية

#### 1. تسجيل الدخول

1. افتح `https://dcbb1029.agri-nile-flow-lake.pages.dev`
2. أدخل البريد: `admin@nawa.eg`
3. انقر "متابعة"
4. اختر الشركة: "نواة المستقبل"
5. أدخل كلمة المرور: `Admin@2025`
6. تحقق من الوصول إلى Dashboard

**النتيجة المتوقعة:** ✅ تسجيل دخول ناجح، عرض KPIs والرسوم البيانية

#### 2. اختبار الموردين

1. من الشريط الجانبي → "الموردين"
2. تحقق من ظهور البيانات المستوردة
3. انقر على مورد لعرض التفاصيل
4. تحقق من عرض كشف الحساب والرصيد

**النتيجة المتوقعة:** ✅ البيانات تظهر بشكل صحيح

#### 3. اختبار الخزينة

1. من الشريط الجانبي → "الخزينة" → "دفتر اليومية"
2. تحقق من عرض جميع الحركات المستوردة
3. تحقق من الرصيد المحسوب بشكل صحيح

**النتيجة المتوقعة:** ✅ جميع الحركات موجودة، الرصيد صحيح

#### 4. اختبار المخزون

1. من الشريط الجانبي → "المخزون" → "الأرصدة"
2. تحقق من عرض الكميات المستوردة

**النتيجة المتوقعة:** ✅ أرصدة صحيحة

#### 5. اختبار التقارير

1. من الشريط الجانبي → "التقارير"
2. تحقق من عرض الأرقام (الموردين، الخزينة، المخزون)
3. انقر "طباعة" للتحقق من صيغة الطباعة

**النتيجة المتوقعة:** ✅ التقرير يظهر بشكل صحيح

#### 6. تغيير كلمة المرور

1. انقر على الأيقونة في أعلى اليمين (صورة المستخدم)
2. انقر "تغيير كلمة المرور"
3. أدخل الحالية ثم الجديدة
4. انقر "تحديث"

**النتيجة المتوقعة:** ✅ تحديث ناجح، ظهور رسالة النجاح

---

### اختبارات قاعدة البيانات

#### التحقق من عدد السجلات

```bash
# من مجلد المشروع الرئيسي
npx wrangler d1 execute agri-nile-flow-data-lake --remote --command "SELECT COUNT(*) AS count FROM suppliers;"
npx wrangler d1 execute agri-nile-flow-data-lake --remote --command "SELECT COUNT(*) AS count FROM cash_transactions;"
npx wrangler d1 execute agri-nile-flow-data-lake --remote --command "SELECT COUNT(*) AS count FROM inventory_movements;"
```

**النتيجة المتوقعة:** ✅ عدد السجلات يطابق المستورد

#### التحقق من التوازن المالي

```bash
# الرصيد الخزينة
npx wrangler d1 execute agri-nile-flow-data-lake --remote --command "SELECT SUM(amount) AS balance FROM cash_transactions WHERE type='IN' UNION ALL SELECT SUM(amount) FROM cash_transactions WHERE type='OUT';"
```

---

## 4️⃣ استكشاف الأخطاء

### مشكلة: "لا توجد بيانات" بعد الاستيراد

**الحل:**
1. تحقق من وجود رسائل خطأ عند التشغيل
2. تحقق من أسماء الـ sheets في `config.js`
3. تحقق من رقم صف البيانات الصحيح

```bash
node import.js suppliers 2>&1 | tee import.log
# اقرأ ملف log للأخطاء
```

### مشكلة: "Sheet not found"

**الحل:**
افتح ملف Excel الخاص بك وانسخ اسم الـ sheet بالضبط (بما فيه المسافات والحالة):

```javascript
// اذا الـ sheet اسمه "الموردين " (مع مسافة في النهاية)
masterSheet: {
  name: 'الموردين ',  // أضف المسافة
  startRow: 2,
}
```

### مشكلة: "column not found" أو بيانات خاطئة

**الحل:**
تأكد من أن رسائل الأعمدة في `config.js` تطابق أعمدة ملفك:

```javascript
// إذا كان كود المورد في العمود B (وليس A)
columns: {
  code: 'B',    // غيّره من 'A'
  name: 'C',    // وغيّر الباقي
}
```

---

## 📋 قائمة الفحص النهائية

- [ ] ✅ Backend مُنشر وجاهز
- [ ] ✅ Frontend مُنشر وجاهز
- [ ] ✅ تسجيل الدخول يعمل
- [ ] ✅ ملفات Excel جاهزة
- [ ] ✅ `migrate/config.js` معدّل حسب ملفاتك
- [ ] ✅ البيانات مُستوردة بنجاح
- [ ] ✅ الموردين يظهرون في التطبيق
- [ ] ✅ الخزينة يظهر رصيدها
- [ ] ✅ المخزون يظهر أرصدته
- [ ] ✅ التقارير تعرض البيانات الصحيحة
- [ ] ✅ تغيير كلمة المرور يعمل
- [ ] ✅ جميع الاختبارات ناجحة ✨

---

**النظام جاهز للاستخدام الإنتاجي!** 🎉
