import XLSX from "xlsx"; import path from "path"; import { fileURLToPath } from "url"; import fs from "fs"; const __filename = fileURLToPath(import.meta.url); const __dirname = path.dirname(__filename); const files = fs.readdirSync(path.join(__dirname, "..")); const filename = files.find(f => f.toLowerCase().endsWith(".xlsx") && f.includes("?????")); const filePath = path.join(__dirname, "..", filename); console.log("File:", filePath); try { const workbook = XLSX.readFile(filePath); console.log("Sheets:", workbook.SheetNames); workbook.SheetNames.forEach(s => { const ws = workbook.Sheets[s]; const data = XLSX.utils.sheet_to_json(ws, { header: 1 }); console.log(`\n--- ${s} ---`); console.log("Rows:", data.length); console.log("Data:", JSON.stringify(data.slice(0, 5), null, 2)); }); } catch (e) { console.log(e.message); }
