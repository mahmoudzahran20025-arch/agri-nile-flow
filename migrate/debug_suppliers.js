import XLSX from 'xlsx'; import path from 'path'; import fs from 'fs'; import { fileURLToPath } from 'url'; const __filename = fileURLToPath(import.meta.url); const __dirname = path.dirname(__filename); const parentDir = path.join(__dirname, '..'); const files = fs.readdirSync(parentDir); const filename = files.find(f => f.toLowerCase().endsWith('.xlsx') && (f.includes('المور') || f.includes('2025-2026'))); if (!filename) throw new Error('Excel file not found'); const filePath = path.join(parentDir, filename); console.log('Reading file:', filename); const workbook = XLSX.readFile(filePath); console.log('Sheet Names:', workbook.SheetNames); workbook.SheetNames.forEach(sheetName => { const worksheet = workbook.Sheets[sheetName]; const data = XLSX.utils.sheet_to_json(worksheet, { header: 1 }); console.log('\n--- Sheet:', sheetName, '---'); console.log('Total Row Count:', data.length); console.log('First 5 rows:', JSON.stringify(data.slice(0, 5), null, 2)); });